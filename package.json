{
  "name": "portfolio-root",
  "version": "2.0.0",
  "private": true,
  "workspaces": [
    "v2"
  ],
  "scripts": {
    "build": "npm run build --workspace=v2",
    "start": "npm run start --workspace=v2",
    "dev": "npm run dev --workspace=v2"
  }
}
